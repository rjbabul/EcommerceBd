import { GetCartItemResponseModel } from './get-cart-item-response-model.model';

describe('GetCartItemResponseModel', () => {
  it('should create an instance', () => {
    expect(new GetCartItemResponseModel()).toBeTruthy();
  });
});
